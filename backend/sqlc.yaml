
version: "2"
sql:
  - engine: "mysql"
    queries: "internal/db/queries/"
    schema: "internal/db/schema/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
#    overrides:
#      - column: scryfall_bulk.scryfall_id
#        go_type: "github.com/google/uuid.UUID"
#      - column: all_cards.scryfall_id
#        go_type: "github.com/google/uuid.UUID"